import 'package:flutter/material.dart';

class BuxoroScreen extends StatelessWidget {
  const BuxoroScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("Buxoroga xush kelibsiz"),),
    );
  }
}