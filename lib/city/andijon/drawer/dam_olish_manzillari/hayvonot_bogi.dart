import 'package:flutter/material.dart';

class HayvonotBogi extends StatelessWidget {
  const HayvonotBogi({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(child: Center(child: Text("Hayvonot bog'i"))),
    );
  }
}