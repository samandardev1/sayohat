import 'package:flutter/material.dart';

class Diskateka extends StatelessWidget {
  const Diskateka({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(child: Center(child: Text("Diskateka"))),
    );
  }
}