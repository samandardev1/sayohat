import 'package:flutter/material.dart';

class Teatr extends StatelessWidget {
  const Teatr({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(child: Center(child: Text("Teatr"))),
    );
  }
}