import 'package:flutter/material.dart';

class IstirohatBogi extends StatelessWidget {
  const IstirohatBogi({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(child: Center(child: Text("Istirohat bog'i"))),
    );
  }
}