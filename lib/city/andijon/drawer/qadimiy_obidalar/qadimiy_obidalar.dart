import 'package:flutter/material.dart';

class MyQadimiyObidalar extends StatelessWidget {
  const MyQadimiyObidalar({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(child: Center(child: Text("Qadimiy obidalar"))),
    );
  }
}